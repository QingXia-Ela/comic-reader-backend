import defineSetting from 'types/settings';

export default defineSetting({
  crypto: {
    active: true,
    key: 114514,
  },
  auth: {
    active: true,
    header_key: 'auth_key',
    header_value: '114514',
  },
});
